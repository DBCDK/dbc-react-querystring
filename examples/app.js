import React from 'react';
import SearchField from '../src/components/searchField.react';

let element = React.createElement(SearchField);
React.render(element, document.getElementById('searchfield'));
